/**
 * Metric definitions for display are built on the server (`server/src/lib/metricDictionary.js`)
 * and included in the AI system prompt. The UI uses exact column names from the upload response.
 */
export const METRIC_DICTIONARY_NOTE =
  "Definitions are computed per uploaded CSV headers on the server.";
